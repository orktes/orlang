// This is to test orlang lint

fn main() {
  for var i = 1; i<; 1 {
    fn foobar() {
      for var i = 0; i; [
        // This is used to test lint/parser error printer
      ]
    }
  }
}

fn otherfn() {
  for {
    fn foobar() {
      for var i = 0; i; {
        var h = ++;
        var b = i++ +;
      }
    }
  }
}

thisisanunexpectedtoken
