// This is to test orlang lint

macro fooMacro {
  ($a:expr, $b:expr) : ($a + $b)
  ($a:expr) : ($a + 1)
}

macro ifMacro {
  ($b:block, $a:expr) : (if $b $a)
}

fn main() {
  var foo = fooMacro!(1, 2) // equals 3
  var bar = fooMacro!(1) // equals 2
  ifMacro!(
    {},
    1 < 0
  )
}
